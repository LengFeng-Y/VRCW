fix(ui): global UX pass - glass-select close animation, spinners, mobile fixes

Root causes fixed:
- glass-select fly-in on close: set opacity:0+transition:none BEFORE
  removeProperty('position') so the element is invisible when the
  containing-block switches from fixed->absolute, killing the cross-screen
  slide animation entirely
- edit modal dropdown misposition: .modal has backdrop-filter:blur(4px)
  which creates a new fixed-position containing block in Chromium; detect
  el.closest('.modal') and use absolute positioning instead (same fix as
  the existing search-box-glass workaround)

New: .btn-spinner CSS class (14px ring, spin keyframe)
Applied to:
  - #btnSearchMain: disabled + spinner during avtrdbFetch / vrcdbFetch
  - #btnSaveEdit: spinner replaces static '...' text during save
  - #btnLogin: spinner + fixed rateLimited finally-bypass bug (finally was
    re-enabling the button immediately, defeating the countdown timer)
  - #tfa-section button: spinner during 2FA verify call

Mobile:
  - avatar filter bar search pill: 42px icon-only -> expands to 140px on
    :focus-within so users can actually type in it
  - .glass-select-options z-index 3000->2500 on mobile (below ctx-menu)
  - world-detail-info-col + world-detail-scroll: overflow visible->hidden
    to prevent long descriptions from blowing out modal width

Accessibility:
  - viewport meta: removed user-scalable=no, maximum-scale 1.0->5.0

v107
